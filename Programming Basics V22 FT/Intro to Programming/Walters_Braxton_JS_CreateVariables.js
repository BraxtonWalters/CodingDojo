// Setting the minimum height requirement var
let minHeight = 42;
// Setting the minimum age requirement var
let minAge = 10;
// setting the rider age to an undefined var
let riderAge;
// setting the rider height to an undefined var
let riderHeight;
